import { AuthShowDirective } from './auth-show.directive';

describe('AuthShowDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthShowDirective();
    expect(directive).toBeTruthy();
  });
});
